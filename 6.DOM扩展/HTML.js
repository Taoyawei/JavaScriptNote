/*
1.getElemetsByClassName()方法，它接受一个参数为类名的字符串

2.classList属性：这个属性作用就是操作元素class属性有四个方法：
	add():将给定的字符串值添加到列表中，如果存在就不添加
	contains()：表示元素class中是否有这个值，有就返回true
	remove()：从元素的class中移除这个值
	toggle()：这个是切换方法，如果列表中就移除这个class如果没有就添加

3.插入标签：两个属性和一个方法：
a:innerHTML:这个属性直接差入职或HTML标签
b:outHTML;也就是插入标签或文本，但是若是原文单位置有这个而原标签，
C:insertAdjacentHTML（）方法，有两个参数，一个为位置，另一个为HTML文本
*/